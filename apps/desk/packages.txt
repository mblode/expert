# apt packages for the desk. One source for both images: apps/desk/Dockerfile
# (local compose) and deploy/fly/Dockerfile (cloud guest) read this file.
ca-certificates
chromium
# curl and git are the Bots' hands outside the browser: four of them read an
# API or land a branch, and a computer where the only way to reach a repo is
# clicking through github.com in Chromium is a slower, worse computer.
curl
dbus-x11
fonts-dejavu-core
fonts-liberation
git
imagemagick
openbox
openssl
procps
python3
sudo
tini
x11-utils
x11-xserver-utils
x11vnc
xclip
xdotool
xvfb
