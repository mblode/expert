---
description: "A static skill shipped with the fixture project, so the sandbox has workspace content and needs a template."
---

# Fixture static skill

The production Bot ships skills like this one. eve seeds them into the
sandbox workspace, which is what gives the sandbox a template key at all: a
project with no skill files needs no template and never trips the prewarm
gap this fixture exists to catch.
